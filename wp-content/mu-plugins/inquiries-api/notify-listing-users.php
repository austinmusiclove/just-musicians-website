<?php
function notify_listings_invited($user_id, $inquiry_id, $listing_ids) {
    // for listing_id in listing_ids
        // get users who have this listing id in their listings meta field
        // for each user
            // create a post of type message that lists the user in the to field, the user_id as the from field and the inquiry_id in the inquiry field
}
