import { expect } from '@playwright/test';
import { test } from '../../../fixtures/fixtures.js';

test.describe('E2E - Forgot Password', () => {

    test.skip('Password reset through forgot password workflow', async ( {} ) => {
        // Create user
        // Login logout
        // Click forgot password
        // Get link from recovery email and follow
        // Put in reset password
        // Log in with new password
        // Fail log in with old password
    });

});
