import { expect } from '@playwright/test';
import { test } from '../../../fixtures/fixtures.js';

test.describe('E2E - Process AIC', () => {

    test.skip('Process AIC on listings page with one listing', async ( {} ) => {
        // Go to listings page with aic logged out
        // Sign up
        // Redirect to same url
        // Expect logged in page
        // Listing created and added to user's listings
        // Listing is draft and author is logged in user
        // Listing is displayed on listings page with draft after name
    });
    test.skip('Process AIC on listings page with two listings', async ( {} ) => { });
    test.skip('Invalid AIC on listings page', async ( {} ) => { });
    test.skip('Expired AIC on listings page', async ( {} ) => { });
    test.skip('No Artists AIC on listings page', async ( {} ) => { });

});
